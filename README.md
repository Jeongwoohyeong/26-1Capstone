# 태양광 모듈 고장 분석 DB 구축 및 분석 툴 개발

## 1. 프로젝트 소개
본 프로젝트는 태양광 모듈에서 수집된 raw data를 기반으로 고장 분석에 활용할 수 있는 데이터베이스를 구축하고, 이를 조회·분석·시각화할 수 있는 오프라인 분석 툴을 개발하는 것을 목표로 한다.

주요 대상 데이터는 I-V 커브, 일사량, 모듈 온도 등이며, 엑셀 형태로 수집된 데이터를 파싱 및 전처리한 뒤 데이터베이스에 저장하고, STC 기준 정규화를 거쳐 사용자가 원하는 조건으로 조회 및 시각화할 수 있도록 구성한다.

본 프로젝트는 산학협력 과제를 기반으로 진행되며, 실시간 수집 시스템보다는 필요 시 데이터를 업로드하여 분석하는 형태의 로컬 기반 도구 개발에 초점을 둔다.

## 2. 과제 목표
본 과제의 목표는 태양광 모듈 고장 분석에 필요한 데이터를 체계적으로 관리하고, 분석 가능한 형태로 가공하여 시각적으로 확인할 수 있는 도구를 구현하는 것이다.

핵심 목표는 다음과 같다.

- 엑셀 raw data를 파싱하여 필요한 데이터를 추출한다.
- 추출한 데이터를 DB에 저장하여 관리 가능한 구조로 정리한다.
- IEC 60891:2021 기반 STC 기준 정규화를 적용한다.
- 특정 날짜 및 시간대 조건에 따라 데이터를 조회할 수 있도록 한다.
- I-V 커브를 케이스별 및 통합 형태로 시각화할 수 있도록 한다.
- 향후 고장 원인 분석 및 결과 내보내기 기능으로 확장 가능하도록 구조를 설계한다.

## 3. 기술 스택
본 프로젝트의 기술 스택은 다음과 같다.

- **UI**: React
- **DB**: SQLite
- **분석**: Python

선정 방향은 다음과 같다.

- **React**는 UI 자유도가 높고 화면 구성이 빨라 오프라인 분석 툴의 프론트엔드 구현에 적합하다.
- **SQLite**는 별도 서버 구축 없이 로컬 환경에서 가볍게 사용할 수 있어 본 과제의 성격과 잘 맞는다.
- **Python**은 데이터 파싱, 전처리, 정규화, 계산 로직 구현에 유리하여 분석 파트에 적합하다.

## 4. 팀 역할 분담
본 프로젝트는 총 3명이 역할을 분담하여 진행한다.

- **UI**: 우형
- **DB**: 소윤
- **분석**: 정우

각 팀원은 담당 영역을 중심으로 개발을 진행하되, 데이터 구조, 입출력 형식, 기능 연결 지점 등은 협업을 통해 조율한다.